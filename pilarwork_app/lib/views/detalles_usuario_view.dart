import 'package:flutter/material.dart';

class DetallesUsuarioView extends StatelessWidget {
  const DetallesUsuarioView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text('Perfil');
  }
}
